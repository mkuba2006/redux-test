import Login from "./login";
import Logout from "./logout";
const Header = () => {

    return (
        <header>
            <Login />
            <Logout />
        </header>
    );
};

export default Header;
